---
title: "WESA counts vs response"
author: "Sarah Popov"
date: "`r Sys.Date()`"
output: pdf_document
---

```{r setup, include=FALSE}
library(ggplot2)
knitr::opts_chunk$set(echo = FALSE,
                      warning = FALSE, 
                      message = FALSE,
                      tidy.opts = list(width.cutoff = 60), 
                      tidy = TRUE)
```

## WESA vs. variable plots

```{r wesa vs year}
ggplot(dat3, aes(x = year_n, y = predicted_wesa, color = n_s)) +
  geom_point(alpha = 0.5) + 
  geom_smooth(se = FALSE) +
  scale_color_manual(values = pal) +
  labs(title = "Year vs WESA count",
       x = "Year", 
       y = "WESA count",
       color = "Station") + 
  cowplot::theme_cowplot()
```

```{r wesa vs dos}
ggplot(dat3, aes(x = dos, y = predicted_wesa, color = n_s)) +
  geom_point(alpha = 0.5) + 
  geom_smooth(se = FALSE) +
  scale_color_manual(values = pal) +
  labs(title = "Day of Season vs WESA count",
       x = "DOS", 
       y = "WESA count",
       color = "Station") + 
  cowplot::theme_cowplot()
```

```{r wesa vs mean temp}
ggplot(dat3, aes(x = mean_temp, y = predicted_wesa, color = n_s)) +
  geom_point(alpha = 0.5) + 
  geom_smooth(se = FALSE) +
  scale_color_manual(values = pal) +
  labs(title = "Mean temperature (C°) vs WESA count",
       x = "C°", 
       y = "WESA count",
       color = "Station") + 
  cowplot::theme_cowplot()
```

```{r wesa vs total_precip}
ggplot(dat3, aes(x = total_precip, y = predicted_wesa, color = n_s)) +
  geom_point(alpha = 0.5) + 
  geom_smooth(se = FALSE) +
  scale_color_manual(values = pal) +
  labs(title = "Total precipitation (mm) vs WESA count",
       x = "mm", 
       y = "WESA count",
       color = "Station") + 
  cowplot::theme_cowplot()
```

```{r wesa vs flow}
ggplot(dat3, aes(x = flow, y = predicted_wesa, color = n_s)) +
  geom_point(alpha = 0.5) + 
  geom_smooth(se = FALSE) +
  scale_color_manual(values = pal) +
  labs(title = "Flow vs WESA count",
       x = "Flow", 
       y = "WESA count",
       color = "Station") + 
  cowplot::theme_cowplot()
```
```{r wesa vs u}
ggplot(dat3, aes(x = u, y = predicted_wesa, color = n_s)) +
  geom_point(alpha = 0.5) +
  geom_smooth(se = FALSE) +
  scale_color_manual(values = pal) +
  labs(title = "u (westerly winds) vs WESA count",
       x = "u (westerly winds)", 
       y = "WESA count",
       color = "Station") + 
  cowplot::theme_cowplot()
```

```{r wesa vs v}
ggplot(dat3, aes(x = v, y = predicted_wesa, color = n_s)) +
  geom_point(alpha = 0.5) + 
  geom_smooth(se = FALSE) +
  scale_color_manual(values = pal) +
  labs(title = "v (southerly winds) vs WESA count",
       x = "v (southerly winds)", 
       y = "WESA count",
       color = "Station") + 
  cowplot::theme_cowplot()
```

```{r wesa vs windspd}
ggplot(dat3, aes(x = windspd, y = predicted_wesa, color = n_s)) +
  geom_point(alpha = 0.5) + 
  geom_smooth(se = FALSE) +
  scale_color_manual(values = pal) +
  labs(title = "Wind speed vs WESA count",
       x = "Wind speed (km/h)", 
       y = "WESA count",
       color = "Station") + 
  cowplot::theme_cowplot()
```

```{r wesa vs tidal amp}
ggplot(dat3, aes(x = elev_range, y = predicted_wesa, color = n_s)) +
  geom_point(alpha = 0.5) + 
  geom_smooth(se = FALSE) +
  scale_color_manual(values = pal) +
  labs(title = "Tidal amplitude vs WESA count",
       x = "Tidal amplitude (m)", 
       y = "WESA count",
       color = "Station") + 
  cowplot::theme_cowplot()
```

```{r wesa vs tide status}
ggplot(dat3, aes(x = tide, y = predicted_wesa, color = n_s)) +
  geom_violin() +
  geom_boxplot() +
  #geom_point(alpha = 0.5) + 
  scale_color_manual(values = pal) +
  labs(title = "Tide vs WESA count",
       x = "Tide", 
       y = "WESA count",
       color = "Station") + 
  cowplot::theme_cowplot()
```